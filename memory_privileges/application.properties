# Vulnerable properties file with memory limit issue
memory.limit=1024M  # Reduced memory limit but still vulnerable

# Privileged execution
user.name=admin  # Still a privileged user

# High file descriptor limit
ulimit=65535  # Hardcoded high file descriptor limit
